#include<stdio.h>
#include<math.h>
// header file includes power amd many other mathematical tools
int main()
{
    int a,b ;
    a=4;
    b=2;
    int power=pow(a,b);
    // a^b will be a bitwise operator called zor
    printf("%d", power);
    return 0;
}